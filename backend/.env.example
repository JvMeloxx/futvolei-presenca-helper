# Configurações do Servidor
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:8080

# Banco de Dados Neon PostgreSQL
DATABASE_URL=postgresql://username:password@host:5432/database

# JWT Secret (gere uma chave segura para produção)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Configurações de Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Configurações de Notificações (para implementação futura)
NOTIFICATION_SERVICE_URL=
NOTIFICATION_API_KEY=

# Configurações de Email (para implementação futura)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@futvolei.com

# Configurações de Push Notifications (para implementação futura)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@futvolei.com